
const nombre = "Monitor 20 pulgadas";
const precio = 300;
const diponible = true;

// ___Un objeto agrupa todo en una sola variable___

// Objet literal
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

console.log(producto);
