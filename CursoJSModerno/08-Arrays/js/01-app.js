const producto = {            //Objeto
    nombre: 'Monitor 20"',
    precio: 300,
    disponible: true,
};

//El arreglo los enumera con un indice que comienza en 0

const numeros = [10, 20, 30]; //Arreglo o array

const meses = new Array('Enero', 'Febrero', 'Marzo');

console.log(numeros);
console.log(meses);

// Arreglo con datos de todo tipo

const deTodo = ["Hola", 10, true, "si", null, {nombre: 'Claudio', trabajo: 'Programador'},[1,2,3]];

console.log(deTodo);
