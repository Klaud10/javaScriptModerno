
let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.3);
resultado = Math.round(2.5);

// Redondear hacia arriba
resultado = Math.ceil(2.5);
resultado = Math.ceil(2.01);

// Redondear hacia abajo
resultado = Math.floor(2.99);

// Raiz cuadrada
resultado = Math.sqrt(2);

// Potencia
resultado = Math.pow(8, 2);

// Valor ABSOLUTO
resultado = Math.abs(-500);

// Mínimo y Máximo
resultado = Math.min(3,6,1,7,0,-2,-8);
resultado = Math.max(3,6,1,7,0,-2,-8);

// Aleatorio
resultado = Math.random();
resultado = Math.random() * 20;

// Aleatorio dentro de un rango...
resultado = Math.floor(Math.random() * 30);


console.log(resultado);